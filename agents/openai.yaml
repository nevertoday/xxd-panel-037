interface:
  display_name: "/xxd-panel-037"
  short_description: "Create Panel 037 from its original style brief."
  default_prompt: "Use $xxd-panel-037; treat its bundled original style brief as the sole aesthetic authority, and ask only for unresolved mode, size, text mode, locale, or wallpaper settings."

policy:
  allow_implicit_invocation: true
